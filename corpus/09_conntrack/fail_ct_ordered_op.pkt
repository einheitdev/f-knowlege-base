name: "ordered comparison on ct_state is a compile error"
source_fw: |
  @xdp(eth0)
  allow if conntrack(pkt).state < established
  default drop
test_packet:
  builder: tcp(dst_port=80)
expected:
  compiles: false
