name: "comparing ct_state against a proto keyword is rejected"
source_fw: |
  @xdp(eth0)
  allow if conntrack(pkt).state == tcp
  default drop
test_packet:
  builder: tcp(dst_port=80)
expected:
  compiles: false
