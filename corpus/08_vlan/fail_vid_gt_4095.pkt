name: "VID literal 5000 above 0..4095 is a compile error"
source_fw: |
  @xdp(eth0)
  drop if pkt.vlan_id == 5000
  default allow

test_packet:
  builder: tcp(vlan_id=10)

expected:
  compiles: false
