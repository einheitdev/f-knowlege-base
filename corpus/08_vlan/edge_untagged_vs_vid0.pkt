name: "untagged frame does not match vlan_id == 0 (distinct from native)"
source_fw: |
  @xdp(eth0)
  allow if pkt.vlan_id == 0
  default drop

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
