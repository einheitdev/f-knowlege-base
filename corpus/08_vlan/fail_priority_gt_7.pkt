name: "VLAN priority literal 8 above 0..7 is a compile error"
source_fw: |
  @xdp(eth0)
  drop if pkt.vlan_priority == 8
  default allow

test_packet:
  builder: tcp(vlan_id=10)

expected:
  compiles: false
