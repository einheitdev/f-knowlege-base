name: "SYN+ACK does not match 'syn and not ack' rule"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.syn and not pkt.tcp.ack
  default allow

test_packet:
  builder: tcp(dst_port=22, syn=true, ack=true)

expected:
  compiles: true
  bpf_action: allow
