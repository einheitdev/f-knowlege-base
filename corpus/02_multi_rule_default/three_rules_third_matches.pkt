name: "third rule fires for icmp when first two don't match"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp
  drop if pkt.proto == udp
  drop if pkt.proto == icmp

test_packet:
  builder: icmp()

expected:
  compiles: true
  bpf_action: drop
