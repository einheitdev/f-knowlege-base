name: "program with only a default rule (no body rules) drops everything"
source_fw: |
  @xdp(eth0)
  default drop

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
