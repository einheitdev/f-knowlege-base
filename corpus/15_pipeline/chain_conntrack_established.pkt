name: "chain conntrack established"
source_fw: |
  @xdp(eth0)
  allow if conntrack(pkt).state == established
  chain rules
  drop if pkt.src_ip == 2.2.2.2
  default drop

test_packet:
  builder: tcp(src_ip="2.2.2.2", dst_ip="9.9.9.9", dst_port=80)

expected:
  compiles: true
  bpf_action: drop
