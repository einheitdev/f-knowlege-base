name: "chain counter then allow"
source_fw: |
  @xdp(eth0)
  count web if pkt.proto == tcp and pkt.dst_port == 80
  chain b
  allow if pkt.src_ip == 5.5.5.5
  default drop

test_packet:
  builder: tcp(src_ip="5.5.5.5", dst_ip="9.9.9.9", dst_port=80)

expected:
  compiles: true
  bpf_action: allow
