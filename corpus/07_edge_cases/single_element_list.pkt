name: "single-element list behaves like equality"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port in [22]
  default allow

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
