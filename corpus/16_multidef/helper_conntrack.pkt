name: "helper conntrack"
source_fw: |
  def ctcheck(pkt):
    if conntrack(pkt).state == established:
      allow
  
  @xdp(eth0)
  def m(pkt):
    ctcheck(pkt)
    drop

test_packet:
  builder: tcp(src_ip="1.1.1.1", dst_ip="9.9.9.9", dst_port=80)

expected:
  compiles: true
  bpf_action: drop
