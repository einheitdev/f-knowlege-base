name: "allow if proto==tcp + tcp packet matches and passes"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp

test_packet:
  builder: tcp(src_port=12345, dst_port=80)

expected:
  compiles: true
  bpf_action: allow
