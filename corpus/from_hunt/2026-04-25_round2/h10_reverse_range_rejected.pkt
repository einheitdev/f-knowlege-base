name: "pkt.dst_port in 100..50 (lo > hi) rejected per spec"
# Spec FWL_V01_SPEC.md:405: range lower bound exceeds upper bound.
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port in 100..50
  default allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: false
