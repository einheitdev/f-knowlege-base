name: "Hex port literal 0x50 == 80 matches dst_port=80"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 0x50
  default allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
