name: "pkt.dst_port in 80..80 single-value range matches"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port in 80..80
  default allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
